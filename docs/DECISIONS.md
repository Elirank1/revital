# V3 Decisions Log

Append-only. Every autonomous call + one paragraph of rationale (mission rule 5). An unlogged decision is a bug.

---

## D-001 · 2026-07-31 · Execution environment: local clone, adapted swarm
The mission prompt targets Claude Code Agent Teams launched from a tmux terminal session. This build runs instead inside a Claude desktop session acting as the lead, with background subagents playing the five teammate roles (same charters, same disjoint file ownership, same rules 2/3/6/13/24/25). Work happens on a fresh local clone at `~/dev/revital` because the canonical checkout lives on Google Drive CloudStorage where `git status` alone times out (>2 min) — a build loop there is not viable. Remote is unchanged (`Elirank1/revital`); only `v3-jump` gets pushed (G1 untouched). The Drive checkout is left untouched as-is.

## D-002 · 2026-07-31 · Repo identity: Elirank1/revital, not the monorepo
Memory said code lives in `Elirank1/monorepo packages/revital`, but the deployed app (Vercel project `revital`, revital-three.vercel.app) is wired to the standalone repo `Elirank1/revital` via the nested `.git` inside the Drive tree. The monorepo copy (`Elirank1/eliran-s-blog` remote) is a stale snapshot. V3 builds on `Elirank1/revital`.

## D-003 · 2026-07-31 · Uncommitted Drive work preserved on `wip-fingerprint-drive`
The Drive working tree carried unpushed work: `src/modules/fingerprint/**` (search-criteria fingerprint module), `api/fetch-url.ts`, and edits to JobInput/ModuleActions/appStore/types. Committed verbatim to local branch `wip-fingerprint-drive` (645eefa) so nothing is lost. It is NOT merged into `v3-jump` — it's an unfinished V2.x feature, unrelated to the V3 contract, and `v3-jump` branches from the deployed `main` per plan §5. Eliran decides its fate later.

## D-004 · 2026-07-31 · gh CLI token invalid; push uses repo-embedded token
`gh auth status` reports an invalid keyring token. Git push for `v3-jump` uses the working token already embedded in the monorepo remote URL (same GitHub account). PR creation via `gh` is unavailable until Eliran re-runs `gh auth login` — not a blocker for any pre-G1 work.

## D-005 (kanban-ui) · 2026-07-31 · Wave-0 Pipeline scaffold: UI-side column model, hook-free view, pure-logic tests
Three autonomous calls inside the kanban-ui file set. (1) The 9-stage column model lives in `src/components/pipeline/stages.ts` (ids + Hebrew/English labels) because `src/types/pipeline.ts` (platform-data, Wave 0) hadn't landed when the scaffold was built; it is UI-render data only — when the canonical types land, the lead reconciles ids (sync or re-export). (2) `PipelineView` is deliberately hook-free in Wave 0 — a pure function of `isV3Enabled()` — so flag-off returns null with zero side effects and tests can invoke it directly. Consequence: no live re-render on flag toggle (reload required), acceptable for a scaffold that platform-data's store supersedes. (3) No DOM test lib exists in the repo (no jsdom/@testing-library), so `PipelineView.test.tsx` tests pure logic per charter task 5: flags helper (default-off, exact-value 'on', storage-less safety), column model (order + exact charter labels + Hebrew-script assertions), and the view via direct invocation with element-tree text collection. Also: `setV3Flag(false)` removes the key rather than writing 'off', so flag-off storage is byte-identical to never-enabled. CONFIG request for DOM test libs filed in BOARD-STATUS.

## D-006 (kanban-ui) · 2026-07-31 · Column model aligned to canonical PipelineStage ids
Platform-data's `src/types/pipeline.ts` landed mid-wave with PascalCase stage ids (`'Sourced'…'Paid'`, `DealStage` adds `'Bench'`/`'Rejected'`). The kanban-ui column model (`src/components/pipeline/stages.ts`) originally used local snake_case ids; it now imports `PipelineStage`/`DealStage` as types and keys every column by the canonical ids (type-only import — kanban-ui never edits `types/`). This closes the reconciliation item flagged in D-005 point (1): there is exactly one source of stage identity, and a unit test asserts `STAGE_COLUMNS` mirrors `PIPELINE_STAGES` from the type contract in order. The UI file keeps only what types/ doesn't own: Hebrew/English display labels and column order.

## D-007 (integrations) · 2026-07-31 · Outreach composer: normalization scope, BiDi policy, hash choice, null-not-throw
Five autonomous calls inside `src/lib/outreach/**`. (1) Phone normalization is Israel-first but not Israel-only: beyond the charter's 05x matrix it accepts landlines (0X-XXXXXXX), 00/+ international prefixes, the common `972 0XX` stray-zero mistake, bare 9-digit mobiles missing the leading 0, and non-IL E.164 numbers — her contact list will contain all of these; anything ambiguous or malformed returns `null` (compose fails, nothing logged, no throw for the UI to handle). (2) BiDi policy: direction OVERRIDE/EMBED/ISOLATE controls (U+202A–E, U+2066–69) are stripped from outgoing message text (spoofing vector, the charter's "no direction-mark injection"), while plain LRM/RLM are preserved in messages (legitimately used in Hebrew text with numbers) but stripped from phone input where RTL UIs leak them. (3) `hashMessage` is FNV-1a 32-bit over UTF-8 bytes, 8 hex chars — deterministic, dependency-free, verified against standard test vectors; it hashes the sanitized text so the stored hash always equals the message actually placed in the href. (4) `composeAndLog` takes an injectable `now` clock (defaults `Date.now`) for deterministic tests, returns the href for the UI to render as an `<a>`, and logs only on successful composition. (5) Email hash input is `subject\nbody` joined over defined parts. G4 verified: no `window.open`/`location`/`fetch` anywhere in the module (grep clean); tests assert hrefs as inert strings in bare node.

## D-008 (agents-engine) · 2026-07-31 · Spend-cap semantics: UTC day buckets, allow-at-limit, fail-open marker, additive response fields
Wave-0 spend guard (`api/_lib/spend.ts`) counts INCR-then-compare: a call is allowed while `count <= limit`, so call N under limit N succeeds and N+1 gets the 429 — simplest boundary that never wastes a paid call on the block itself. Day buckets are UTC (`toISOString` slice) so serverless regions agree on "today"; TTL 48h self-cleans keys across the boundary. Limits come from `SPEND_CAP_CLAUDE`/`SPEND_CAP_ENRICH`/`SPEND_CAP_GEMINI` with defaults 200/40/40; invalid or non-positive env values fall back to defaults rather than disabling the cap. On Redis failure or missing `KV_REST_API_URL`/`KV_REST_API_TOKEN` the guard fails OPEN (charter mandate) and the handlers surface `spendGuard:'unavailable'` as an additive field on their success JSON (including the Anthropic passthrough in `api/analyze.ts` — clients ignore unknown top-level fields, so flag-off behavior stays byte-identical whenever Redis is healthy). 429 bodies carry an English `error`, a Hebrew `message`, and a `spend:{service,count,limit}` object. The spend key uses the access code verbatim (matches the existing `revital:data:${code}` pattern); empty code buckets as `default`, same as `api/data.ts`.

## D-009 (agents-engine) · 2026-07-31 · Cron auth accepts Vercel Bearer convention and fails closed; audit types local pending pipeline.ts
`requireCronSecret` (`api/_lib/guard.ts`) accepts `Authorization: Bearer <CRON_SECRET>` — the header Vercel cron sends natively — plus an explicit `x-cron-secret` for manual invocation, and fails CLOSED with 503 when `CRON_SECRET` is unconfigured: an unprotected tick must never run, even in dev. `api/agents/tick.ts` checks auth before the preview guard so probing a preview deployment leaks nothing about env state. `src/lib/audit.ts` defines its own minimal `AuditEvent` types because `src/types/pipeline.ts` did not exist when written; marked `// TODO(lead): unify with types/pipeline.ts` per charter. Rotation assumes append-order logs (guaranteed by the append-only rule), drops oldest-first per (entityType,entityId) bucket at N=500, and returns the same array instance when nothing drops to avoid store churn.

## D-010 (quality-gate) · 2026-07-31 · Legacy smoke: api/data.ts tested via mocked handler; storage-only caps pinned as-is
Two autonomous calls. (1) `api/data.ts` mergeById is not exported, but the default handler IS importable, so instead of a FIX line the suite tests it HTTP-level: `@upstash/redis` is vi.mocked with an in-memory Map and the handler is driven with fake VercelRequest/Response objects — pinning auth (401/405/400), the empty-blob GET shape, id-overwrite merge, timestamp/createdAt newest-first ordering, id-less item drops, and the 100/50/200 caps without ever touching the live blob (G2). Note `api/` sits outside tsconfig `include`, so this file runs under vitest but not `npm run typecheck` — same treatment as `api/*.ts` today. (2) The V2 store caps are storage-only: `saveJob`/`addAnalysis`/`addToLog` persist `slice(0, cap)` but set the UNSLICED array into Zustand state, so in-memory lists can exceed 50/100/200 until reload. The smoke suite pins this asymmetry explicitly as documented legacy behavior rather than "fixing" the tests to a cap the code doesn't enforce — platform-data should decide whether V3 slices normalize it.

## D-011 (agents-engine) · 2026-07-31 · audit.ts now imports AuditEvent from types/pipeline.ts (amends D-009)
`src/types/pipeline.ts` landed (platform-data) after `src/lib/audit.ts` was first written, so per charter the audit module now imports the canonical type instead of defining a local one. `audit.ts`'s `AuditEvent` extends the pipeline one with two refinements, both flagged `TODO(lead)` for the Wave-0 type handoff: (1) `entityType`/`entityId` are narrowed from optional to required — rotation buckets per (entityType,entityId) and cannot type-safely rotate events that lack them; (2) an optional `agent` field is added because the plan §2 attribution chips ("Screener 07:14 — scored 82") need the agent name and the canonical type omits it. Every audit.ts event remains assignable to the pipeline.ts `AuditEvent`, so store/persistence code compiles unchanged. Suggested lead resolution: fold both refinements into pipeline.ts and delete the local extension.

## D-012 (platform-data) · 2026-07-31 · v3 blob layout: same Redis key, additive `v3` field, GET strips it for legacy clients
Three related autonomous calls in the `/api/data` v2 proposal (`docs/diffs/api-data-v2.ts` — not applied; lead reviews line-by-line). (1) v3 data lives inside the existing `revital:data:${code}` blob under a single additive `v3` field rather than a second Redis key: auth, snapshot/restore, and the size guard stay single-keyed, and the change is purely additive; splitting to a separate key later is itself additive if the blob nears the cap. (2) GET responses *strip* the `v3` field unless the client opts in via `X-Revital-V3: 1` (or `?v3=1`) — strictly stronger than relying on legacy clients ignoring unknown fields: the legacy wire response stays byte-identical to current production, which is what charter criterion (a) actually demands. (3) Blob-size guard values: warn at 800KB (additive `sizeWarning` response field), hard cap 1.5MB returning 413 + `alert` — and the hard cap applies only to POSTs carrying v3 data, so the legacy V2 sync path can never be blocked by the new guard. Legacy-only POSTs carry the stored v3 section forward untouched, so V2 sync cannot wipe v3 data either.

## D-013 (platform-data) · 2026-07-31 · Version semantics: blob-level vCounter, v=0 = never-synced, equal-v POST wins, merge logic single-sourced with an inlined server copy
LWW ordering uses one monotonic `vCounter` per blob (not per record): simpler, strictly ordered, and any accepted write anywhere advances it — per-record counters add nothing under a single-blob store. Client-created records carry `v: 0` (never synced); an edit keeps the record's last-pulled server `v` as its implicit base. Server rule: `incoming.v >= stored.v` ⇒ apply + stamp `++vCounter`; `incoming.v < stored.v` ⇒ drop as stale (server wins; client converges on next GET). Equal-v accepts are intentional: they mean the client has seen the latest server state, and they are what makes one-click undelete possible (a client that has *seen* a tombstone at v=N may POST an undeleted copy at v=N). Stale live copies can never resurrect newer tombstones — unit-tested. The algorithm is single-sourced in `src/lib/persistence/merge.ts#serverMergeCollection` (15 unit tests) and inlined verbatim into the api-data-v2 proposal with a `KEEP IN SYNC` marker, because the serverless function must stay self-contained rather than importing from `src/` through the Vercel bundler. POST responses echo `{vCounter, accepted, staleDropped, records}` so the client adopts server-stamped versions without an extra GET.

## D-014 (platform-data) · 2026-07-31 · Store hygiene: Rejected-without-reason refused, name-only dedupe never auto-merges, dirty-ids drive push, snapshots git-ignored
Four smaller calls. (1) `moveDealStage(id, 'Rejected')` without a reason is a silent no-op — plan §2 makes the reason mandatory and a throwing store action would crash drag handlers; the UI layer owns prompting. (2) `findDuplicatePerson` treats phone/email as globally unique identifiers (exact match ⇒ same Person even under a different name — marriages, nicknames, transliteration), while a name-only collision returns a `name_only` result that callers must surface as a `merge_person` Suggestion, never auto-merge — Hebrew names collide too often. (3) The store tracks client-only `dirtyIds` per collection; `buildPushPayload()` sends only dirty records and `markSynced()` clears them — keeps POSTs small and makes the equal-v LWW rule safe (only actually-edited records are ever re-sent). (4) `scripts/data/snapshots/` is git-ignored via `scripts/data/.gitignore` (my file set): snapshots contain candidate PII and must never land in the repo (plan §5 privacy). Also noting D-010's storage-vs-memory cap asymmetry in the V2 store: v3 slices persist exactly what they set into state, so the asymmetry does not carry over.

## D-015 · 2026-07-31 · Push to GitHub blocked — no valid write credential on this machine
`Elirank1/revital` is public, which is why reads (clone/ls-remote) worked; the ghp_ token embedded in the monorepo remote and the keychain credential are both rejected for push ("Invalid username or token"). Pushing `v3-jump` (and the Vercel preview deploy) waits for Eliran to run `gh auth login -h github.com` or supply a PAT with repo-write. All local work continues; commits accumulate on the local `v3-jump` and the `v3-wave0` tag.

## D-016 · 2026-07-31 · Wave 0 checkpoint passed; Wave 1 opened
Checkpoint green: typecheck clean, 214/214 unit tests, production build passes, visual smoke verified in the browser (flag off ⇒ legacy byte-identical, no console errors; flag on ⇒ board scaffold renders BiDi-correct with 'לוח' nav active). Tag `v3-wave0`. Wave 1 opened with a lead-defined binding store contract (docs/waves/wave1-store-contract.md) to cut the kanban-ui⇄platform-data dependency (rule 19); dnd-kit + jsdom/@testing-library installed by the lead (CONFIG request from kanban-ui honored); vitest default env stays node — DOM tests opt in per-file via `// @vitest-environment jsdom` so Wave-0 node tests keep their exact semantics.

## D-017 (integrations) · 2026-07-31 · Wave-1 drafts: local SuggestionInput, grounded-Hebrew policy, glue null-contract, hash single-sourcing
Five autonomous calls inside `src/lib/outreach/**`. (1) The store contract's `SuggestionInput` is not yet exported anywhere (platform-data builds concurrently), so `drafts.ts` defines it locally as `Omit<Suggestion, envelope | status | timestamps>` — the semantic core {agent, kind, dealId?, personId?, title, body, evidence}, structurally assignable to any store implementation that generates ids/versions itself; `TODO(lead)` marks the re-point once the canonical export lands. (2) Grounded-Hebrew policy: draft text interpolates ONLY fields present on the passed Person/Deal/Analysis; the one constant is the signature "רויטל קרן" (the product's sole user, per mission contract). With an analysis the opener may say the profile was reviewed (the analysis IS that review) but never mentions the score or verdict — those go into `SuggestionEvidence` claims instead, which is exactly what evidence is for. Without an analysis the opener claims no review. `followUpDraft` with `daysSilent <= 0`/NaN drops the time reference rather than inventing one; day grammar follows Hebrew numerals (אתמול/יומיים/X ימים/11+ יום). (3) BiDi: every interpolated field AND final text pass `sanitizeMessageText` (strips override/embed/isolate controls); no direction marks are ever added — mixed-direction rendering is the UI's `dir="auto"` job. (4) Glue null-contract: `suggestionToWaHref`/`suggestionToComposeArgs` return `null` (never throw) for non-`draft_message` kinds, empty body, or missing/unusable phone, mirroring Wave-0 `composeWaMeUrl`; `draftToSuggestion` returns a defensive copy with `body` pinned to `draft.text`. (5) Hash single-sourcing: the rendered href, `suggestionMessageHash`, and click-time `composeAndLog` all hash the same suggestion body — round-trip unit-tested so the logged hash can never drift from the accepted draft. G4: self-grep test (comment-stripped, call-shaped tokens) plus `scripts/gate/check-no-send-paths.sh` clean; 97 outreach tests / 245 total green.

## D-018 (platform-data) · 2026-07-31 · logContact persists the channel ahead of the type; ContactLogger adapter bridges epoch→ISO
The binding contract's `logContact(personId, channel, messageHash, ts?)` must not lose the channel, but `ContactEvent` in the lead-owned `src/types/pipeline.ts` has no `channel` field. Rather than editing the lead's file, the store writes the channel into the persisted JSON via a type assertion (invisible at type level, present in the data) and mirrors it in the audit entry's `after` payload — forward-compatible the moment the type gains the field. **Proposed lead change:** add `channel?: 'whatsapp' | 'email'` to `ContactEvent`. Same handoff note for `PersonInput`/`DealInput`/`SuggestionInput`/`AddPersonResult`, which live in `src/store/pipelineStore.ts` for now and can be hoisted into types/. The exported `pipelineContactLogger` implements `src/lib/outreach`'s `ContactLogger` (epoch-ms `ts` → ISO) so `composeAndLog` captures contacts with zero keystrokes, per plan §2.

## D-019 (platform-data) · 2026-07-31 · acceptSuggestion applied-effect scope in Wave 1
Accepting is the only path where agent output mutates card state (plan §3). Wave-1 applied effects: `next_action` with a `dealId` stamps `deal.nextAction = { label: title, owner: 'revital' }`; `draft_message` mutates nothing (the UI renders the accepted draft as a `wa.me` link via the integrations glue); `merge_person` acceptance records the decision in status/audit only — the actual person merge is a destructive multi-record operation that ships with its dedicated merge flow (Wave 2), not as a side effect of a queue tap; `new_card` is unused because the Screener's sanctioned Act creates cards directly through `addPerson`/`addDeal`. Accepts (and moves) push an in-memory undo snapshot; dismissals do not (re-filing a suggestion is not destructive). `resolveSuggestion` remains as a deprecated delegate so Wave-0 callers behave identically.

## D-020 (platform-data) · 2026-07-31 · Contract semantics: duplicateOf on both dedupe kinds, addDeal throws on unknown person, creation skip-events, undo keeps server v
Autonomous calls inside the binding signatures. (1) `addPerson` returns `duplicateOf` for BOTH dedupe outcomes: exact hit ⇒ `person` is the existing record merged additively (existing fields win, missing ones filled, analysisIds unioned) and `duplicateOf` is the pre-merge copy; name-only ⇒ `person` is NEW, `duplicateOf` is the collision, and one pending `merge_person` Suggestion is filed (guarded against duplicates so re-runs stay idempotent). (2) `addDeal` THROWS on an unknown/tombstoned `personId` — that is API misuse by a programmer, unlike Rejected-without-reason which is a user-flow refusal and returns null from `moveDeal` (D-014). `addDeal` with an existing deterministic id is an idempotent no-op returning the existing deal (screener/backfill re-runs). (3) Every `addDeal` logs a creation StageEvent (`from: null`); entry at a later pipeline stage records all earlier stages as `skippedStages` so conversion stats exclude intervals the deal never sat in. (4) Undo: in-memory stack (cap 20, not persisted — a same-session affordance behind a 6s toast); restore keeps the CURRENT record's `v` so an undo after a sync round is an ordinary LWW edit, never a stale write; undoing a move appends a compensating StageEvent (`actor: 'system'`, `reason: 'undo'`) so history stays append-only. (5) `moveDeal` to `Bench` benches the Person like `Rejected` does, so `benchPersons()` is truthful for both rails.

## D-021 (platform-data) · 2026-07-31 · Backfill simulation + sync response adoption
Backfill (`src/lib/backfill.ts`): the dry-run simulates person resolution over live persons plus persons planned earlier in the same run, grouping analyses by legacy `candidateId` (same candidateId ⇒ same Person, definitionally); cross-candidate name collisions are never auto-merged — the plan says create + flag, matching store behavior. Deterministic ids `p_bf_<candidateId>` / `d_bf_<analysisId>` make re-runs no-ops, and tombstoned backfill cards are treated as carded (never resurrected). Apply writes one latest-run backup to `revital_v3_backfill_backup` (overwritten per run — unbounded backup growth would threaten the blob budget; the real rollback remains flag-off). Cards land in **Screened only** (plan §2). Sync (`src/lib/persistence/sync.ts`): push adopts server-stamped records straight from the POST echo (`v3.records`, D-013) instead of a follow-up GET, then `markSynced()` clears all dirty ids — acceptable because Wave-1 sync runs are user-triggered and serialized; 413 (`alert`) and 503 preview read-only surface as structured results, never thrown into UI paths; everything is a no-op while `revital_v3_flag` is off.

## D-022 (agents-engine) · 2026-07-31 · Screener: id scheme, cross-path dedupe, verdict-agnostic carding, person-id collision fallback
Four autonomous calls in `src/agents/screener.ts`. (1) Deterministic ids use screener-owned prefixes (`p_scr_<candidateId>` / `d_scr_<analysisId>` / `s_scr_flags_<analysisId>`) rather than reusing backfill's `p_bf_`/`d_bf_` — provenance stays readable in the data, while cross-path dedupe is guaranteed by `analysisAlreadyCarded`, a strict superset of backfill's check (deal.analysisId link + both deterministic deal-id families, tombstones count as carded). Either path can run first, in any interleaving, and the same analysis never double-cards — unit-tested in both directions, including a screener-card blocking `backfillDryRun`. (2) Person resolution order: a live person under either deterministic id for the candidateId is reused directly ("same candidateId ⇒ same Person, definitionally", D-021) — necessary because `addPerson`'s exact dedupe requires a phone/email hit that legacy analyses never carry, so a name-only re-add would wrongly create a duplicate + merge flag for the SAME candidate. Tradeoff: direct reuse cannot union the new analysis id into `person.analysisIds` (the Screener has no sanctioned person-edit power); the deal's `analysisId` keeps the evidence link. (3) Carding is verdict-agnostic (Reject verdicts card too, matching backfill) because rejection is HER decision — instead, a Reject verdict always yields a 'flag' Suggestion so the reasoning is one tap away; flag suggestions fire only at card time (idempotent by construction) and interpolated analysis text passes `sanitizeMessageText` (D-017 BiDi policy). (4) If the deterministic person id is already taken (tombstoned, or a renamed-candidate edge), the Screener falls back to a store-generated id — never resurrects, never double-assigns an id.

## D-023 (agents-engine) · 2026-07-31 · attachScreener: baseline-not-backfill, flag-gated retry queue, read-only legacy subscription
`attachScreener(appStore, pipelineStore?, opts?)` snapshots the analysis ids present at attach as a seen-baseline and cards only analyses that arrive AFTERWARD (new CV analyzed, or cloud sync while the app is open). Default behavior deliberately does NOT card pre-existing analyses: bulk-carding history is exactly what backfill does behind its dry-run + `{confirm:true}` gate (plan §2), and an event hook must not become a silent backfill at app boot — `opts.processExisting: true` exists for the lead to opt in consciously. Runs blocked by the v3 flag keep their analyses in a pending set retried on the next analyses change (idempotency makes retries free), so a CV analyzed moments before the flag flips on is not lost; a flag toggle alone does not trigger a run (no pipeline-store subscription — deliberate, to keep the hook single-sourced on legacy events). The legacy store is consumed strictly through `getState()`/`subscribe()` via a structural read-only interface — the Screener cannot even express a legacy mutation; a unit test pins state-reference identity across an attach+run.

## D-024 (kanban-ui) · 2026-07-31 · Wave-1 board architecture: zero new view keys, reactive flag, read-only legacy subscription, Rejected not droppable
Four autonomous calls inside the kanban-ui file set. (1) The board hosts the Wave-1 surfaces itself — TodayView as a "היום" tab and SuggestionsQueue in an inbox panel behind the header's "הצעות" badge — so Wave 1 needs ZERO new AppView keys or App.tsx edits; both components are also exported standalone with optional wiring documented in the READMEs, and the lead can promote them to real views anytime. (2) PipelineView now reads the flag from `usePipelineStore((s) => s.v3Enabled)` instead of the Wave-0 `flags.ts` helper: same `revital_v3_flag` key, but reactive — `setV3Flag(true)` re-renders live with no reload; flag-off still returns null from the FIRST hook with zero store mutations, and `flags.ts` remains for store-free consumers (App nav). (3) The match score/verdict chip needs the legacy analysis behind `Deal.analysisId`, so the board subscribes READ-ONLY to `useAppStore((s) => s.analyses)` — kanban-ui never writes the legacy store; the alternative (lead passes analyses as props) was more wiring for the same coupling. (4) `'Rejected'` is excluded from droppable targets (`dragEnd.ts` DROPPABLE_STAGES = 9 columns + Bench): the store refuses Rejected-without-reason (D-014) and a drag gesture cannot carry a reason — reject-with-reason gets a dedicated flow in a later wave; dropping on Bench IS allowed and benches the person (store D-020 semantics). Drag-end logic is a pure exported resolver (`handleBoardDrop`) so moveDeal wiring unit-tests in node without dnd simulation, exactly per the task list.

## D-025 (kanban-ui) · 2026-07-31 · Card outreach semantics: injectable logger, empty-chat fallback, hash-faithful draft links; Wave-0 view tests upgraded to jsdom
Four more calls. (1) DealCard is prop-driven with an injectable `ContactLogger` (prod passes `pipelineContactLogger`, tests pass a stub) and an injectable clock for deterministic aging — DOM tests run without store or dnd scaffolding. (2) The one-tap wa.me button: with an accepted `draft_message` the anchor href carries the draft body and the click-time `composeAndLog` logs the SAME hash as `suggestionMessageHash` (glue guarantees, round-trip-tested); with no accepted draft the anchor still renders as a bare `https://wa.me/<E164>` chat-opener and logs `hashMessage('')` — opening the chat IS a contact worth capturing, and hiding the button until a draft exists would cost Revital her fastest action. Missing/unusable phone ⇒ no anchor at all (composer null-contract). Navigation is exclusively the human-clicked `<a href>`; the onClick only logs (G4 grep + gate clean). (3) Aging ring is a conic-gradient CSS ring that fills toward the red threshold (10d = full), thresholds pinned in `aging.ts` (amber ≥5, red ≥10) with `data-aging` for tests; invalid/future `stageEnteredAt` reads as 0 days — a card render must never crash on bad data. (4) The Wave-0 pure-invocation view test (React-element-tree walking, pre-jsdom) is retired: PipelineView.test.tsx is now a real jsdom suite over the live store (columns/counts/move+undo round-trip/badge/tabs/calibration "no ₪" assertion), and the Wave-0 flags+column-model assertions moved intact to `stages.test.ts` (node). ΣEV renders as the literal placeholder "ΣEV —" — counts only until Wave-2 fees, no invented figures.

## D-026 (quality-gate) · 2026-07-31 · Wave-1 verification: seam-level smoke over re-testing, strict any-arg nav gate in UI/agent dirs, the gate gets its own test, jsdom navigation blocked not simulated
Four autonomous calls. (1) The Wave-1 quality-gate suites test SEAMS, not modules: every owner already unit-tests their own surface (378 green at handoff), so re-asserting store actions or card markup would be duplication, and the uncovered risk is the joints — screener output rendering through the board's read-only legacy subscription, the draft→accept→wa.me hash chain crossing three owners, undo observed simultaneously on queue AND card, and the flag gate proven inert across ALL entry points at once (App render + legacy actions + screener + confirmed backfill + sync, with localStorage key-namespace snapshot-diffing and a fetch spy). Single-writer is pinned positively (audit + store swap) and negatively (pre-click objects deep-frozen; any in-place component mutation would surface). (2) The extended send-path gate applies the strict any-argument patterns (`window.open(`, `location.href =`, `location.assign(`/`replace(`, `sendBeacon(`) ONLY under src/views/ and src/agents/ per the task scope — src-wide they stay wa.me/mailto/WhatsApp-shaped, because a future legacy component could legitimately window.open a blob (print/export) while UI/agent dirs never may; `fetch(` to wa.me was added src-wide. A grep confirmed zero existing call-shaped hits anywhere in src/, so the strict rule lands with no exemptions. (3) The gate script itself is now under test (scripts/gate/check-no-send-paths.test.ts): the script is copied into a temp fixture tree (it resolves src/ from its own location, so the repo is never touched) and must exit 2 naming the label for every violation class and 0 on clean/test-quoted trees — a gate that silently rots is worse than no gate. (4) DOM tests assert wa.me anchors as inert hrefs and block jsdom navigation with a capture-phase preventDefault listener rather than mocking window.open or simulating navigation — G4's "assert hrefs without navigating" as literal test mechanics. Result: typecheck clean, 30 files / 407 tests green, gate exit 0.

## D-027 · 2026-07-31 · Wave 1 closed; Vercel not git-connected; agent-store bridge
Wave-1 checkpoint green (407/407, build, G4 gate) — tag v3-wave1. Discovery: Vercel project `revital` is CLI-deployed, not git-connected, and the production bundle contains the fingerprint WIP — GitHub main is behind production, so connecting Git before fast-forwarding main would regress her app; packaged as G1-lite for Eliran (BOARD-STATUS GATE-WAIT). Wave 2 opened with binding contracts (docs/waves/wave2-contract.md): pre-G3 the server tick bridges suggestions/agent_runs through the existing Redis v3 section behind an AgentStore interface (Supabase schema+importer still built, mocked, for the G3 swap) — single-writer preserved, no LLM in cron, spend caps untouched.

## D-028 (platform-data) · 2026-07-31 · Money/EV semantics: null-over-zero everywhere, past-Screened calibration, observed-outcome blending, Bench refuses a number
Autonomous calls inside the binding Fee/EV contract (`src/lib/money/`). (1) `feeAmount` returns null for EVERY incomplete/degenerate shape — missing fields, 0, negative, NaN, Infinity — a 0% or ₪0 fee is treated as "not yet captured", never as a computable zero (calibration hard rule). (2) "Past Screened" = strictly after Screened in the nine-stage order (Outreach+); Bench and Rejected never calibrate; tombstoned deals never calibrate. (3) `effectiveProbability`'s observed stats (`observedStageStats`) count RESOLVED deals only: a deal that reached Paid is a success for every stage it actually entered, a deal terminally in Rejected is a failure for those stages; in-flight and Bench deals are censored, and skipped stages contribute nothing (they never appear as an event's `to`) — unfinished journeys must not fake conversion rates. Blend at n≥10 is 50/50 midpoint↔rate per contract; `effectiveProbabilityRange` collapses to a point once blended (display-as-range rule). (4) `dealEV`: Rejected → 0 (truthfully dead), Bench → null (no probability model for the parked rail — refusing to invent one), tombstone → null, and a finite `deal.probabilityOverride` in [0,1] wins over the stage probability. (5) `qualifiedPipeline` enforces calibration in the DATA layer, not just the UI: uncalibrated mandates contribute to no figure; headline is null only when zero calibrated mandates exist (a calibrated mandate with nothing past Submitted reads a truthful 0 + early-range footnote). Paid deals stay in the Submitted+ sum at p=1 (realized ₪ is still pipeline money until the UI segments it). (6) Fees/priors persist locally (`revital_v3_fees`/`revital_v3_priors` — "per access-code" satisfied by the per-user localStorage namespace); MandateFee is NOT in `PipelineSyncPayload` (lead-owned type untouched) — history reaches Supabase via the importer's fee ledger. Fee/prior edits audit through the pipeline store's append-only log. `setFee` defaults `guaranteeDays` to 0 — no invented guarantee. MandateFee (mandate-level, `invoiceStatus: 'due'`) supersedes the Wave-0 per-deal `FeeTerms` for ALL money math; `Deal.fee` is never consulted.

## D-029 (platform-data) · 2026-07-31 · Metrics definitions, agent-store importer shape, mock-enforced append-only, CSV stage wiring
(1) `computeLeadingIndicators` (`src/lib/metrics/leadingIndicators.ts` — new dir, no owner collision): timeToFirstTouch = MEDIAN days from a deal's creation StageEvent to its first attributable 'contacted' event (dealId link wins, personId via optional `opts.deals`; pre-carding contact clamps to 0); slaHitRate = 3d first-touch SLA over deals created into Sourced..InConversation, where untouched-and-older-than-SLA counts as a miss and untouched-younger is pending/excluded; clientFeedbackLatency = median days from entering Submitted to the deal's next transition (still-waiting = censored); suggestionEditRate reads an `editedBeforeAccept` boolean riding ahead of the lead-owned Suggestion type (D-018 pattern — kanban-ui stamps it at accept time) and returns null until that instrumentation exists: absence of the marker is "not measured", never "0% edited". Every metric is null on insufficient data. (2) Importer (`src/lib/persistence/agentStoreImporter.ts`): same safety geometry as backfill (dry-run-first, flag+`{confirm:true}` gate, latest-run pre-migration backup of the DESTINATION tables under `revital_v3_agent_import_backup`); events are append-only — an existing id with different content is reported as a conflict and skipped, never overwritten; fee history lands as `mandate_fees` (current state) + `fee_ledger` (append-only, deterministic id `fl_<jobId>_<updatedAt>` ⇒ idempotent re-runs); all reads/writes scoped by access_code. `buildImportSource` sends agentRuns empty — the client holds no runs in Wave 2 (tick writes them bridge-side per contract). (3) `mockSupabase.ts` mirrors schema.sql's invariants in memory (PK rejection, append-only upsert rejection, copy-on-read) so importer tests exercise what the real DB will enforce; `supabase/schema.sql` has NO persons/deals tables by design (single-writer, plan §3), append-only triggers on events/fee_ledger, RLS enabled with zero policies (service-role only pre-G3). (4) CSV (lead-granted `src/utils/export.ts`): `Stage` column appended last per the Wave-2 grant, legacy columns pinned byte-identical by test; the cell resolves via `Deal.analysisId` (live deals only, latest-updated wins) and is EMPTY when the v3 flag is off or no deal exists — the store is consulted read-only and only while the flag is on; `deals` is injectable so callers/tests bypass the store.

## D-030 (integrations) · 2026-07-31 · Client Reporter: typed claim-lines prove evidence coverage, client-facing funnel buckets, triple-gated polish stub
Autonomous calls inside the Wave-2 Client Reporter contract (`src/reporting/`). (1) Evidence coverage is architectural, not aspirational: `buildMandateReport` assembles BOTH renderings (RTL HTML + plain text) exclusively from typed `ReportLine`s — a line is either a static `frame` (greeting/section headers/sign-off) or a `claim` whose non-empty `refs` tuple makes a ref-less claim unrepresentable in the type system (runtime throw as backstop). Tests prove the property globally: the plain text reconstructs 1:1 from the lines, frames must match a static whitelist (so data can ONLY flow through claims), and `evidence` is asserted to be the exact claim×ref flattening. Aggregate claims (funnel counts, next steps) cite EVERY contributing dealId; candidate lines cite deal+person+StageEvent. (2) The report is client-facing, so the nine internal stages compress into six client-readable funnel buckets and internal money state never leaks: Paid renders as "השמה הושלמה", invoice/₪ words never appear; Bench is excluded; Rejected appears only as an aggregate screened-out count; the opening "in active process" count honestly excludes completed placements. Submitted dates prefer the StageEvent (skip-events count via `skippedStages`), falling back to `stageEnteredAt`; feedback nudges fire past 6 days at Submitted/ClientInterview (Pit Boss threshold, overridable). Candidate summaries come only from an existing `CandidateAnalysis.profileSummary` (trimmed, cited) — no analysis, no summary, no invented facts. Deterministic throughout: injected clock, UTC date parts, stable sorts; all interpolation passes `sanitizeMessageText`, HTML is escaped with `dir` attributes + logical CSS (no direction marks ever added). (3) `booleanStrings` emits per-script (he/en) variants — core (title AND per-pillar OR-groups, CRITICAL+HIGH), `site:linkedin.com/in` X-Ray, and a broad recall query (+MEDIUM); LOW keywords are noise and never included; caps (8/group, 4 groups, 12 broad), case-insensitive dedupe, quote/BiDi stripping keep queries paste-clean and byte-deterministic. (4) `polishWithClaude` is triple-gated: `enabled === true` (strict, default OFF), an INJECTED `fetchImpl` (deliberately no global-fetch fallback — the stub is physically incapable of network unless handed a transport), and a non-empty `accessCode` (proxy spend caps). It polishes plain-text wording ONLY — `html`/`lines`/`evidence` return untouched so claims keep their refs — and falls back to the original report on any failure; the report must always be deliverable. Exposed via `suggestionInput` (agent `client_reporter`, kind `report`) so output reaches the UI only through the Suggestion queue (single-writer). Also exported the existing `daysAgoHebrew` from `src/lib/outreach/drafts.ts` for reuse (own file set). Typecheck clean; 53 new tests, full suite 512 green.

## D-031 (agents-engine) · 2026-07-31 · Wave-2 loop: bridge rails enforced in code, episode-keyed idempotency, honest server inputs, uncalibrated deals rank money-free
Autonomous calls inside the binding Wave-2 tick/Pit Boss/bridge contracts. (1) **Bridge rails are structural, not promised** (`api/_lib/agentStore.ts`): `AgentStore.write` accepts ONLY suggestions+agentRuns; `redisBridgeStore` rebuilds the blob from a fresh read taken immediately before the set, carrying persons/deals/events and every legacy byte through untouched — there is no code path by which an agent write can reach card state (tested with a hostile payload smuggling `persons`). Versions are server-assigned via the canonical `serverMergeCollection` imported from `src/lib/persistence/merge.ts` (api/data.ts inlines the same function — one semantics, two entry points); agentRuns rotate to ≤200 newest-by-v with physical removal (a bounded bridge blob is the point); the bridge honors api/data.ts's 1.5MB hard cap and REFUSES oversize writes; unlike the spend guard this write path fails CLOSED when Redis is unconfigured. The Supabase-shaped impl (`supabaseAgentStore` over `SupabaseAgentClient`, `mockSupabaseAgentStore` for tests) reuses the importer's row shapes (payload column = canonical record) so the post-G3 swap changes a constructor, not semantics; it does not rotate — retention in a real DB is schema policy, not client pruning. (2) **Idempotency = deterministic episode ids + LWW staleness**: SLA suggestions key on (dealId, last-contact ts) `s_sla_*`, Pit Boss on (dealId, stageEnteredAt) `s_pb_*`; a daily re-run re-emits the same ids with v=0, the merge drops them as stale, so re-runs are free AND a dismissal stays final for its episode (no resurrection — verified at bridge, sweep, and handler levels). This also resolves the contract's "NEW/CHANGED records only" cursor honestly: aging and silence advance with the CLOCK, not with record versions, so the passes re-evaluate every live deal each tick while the recorded per-run `cursor` (blob vCounter at processing, riding ahead of the lead-owned AgentRun type like D-018's channel) keeps runs observably incremental and cheap-by-idempotency. Tick chunking: first code always makes progress, budget checked before each subsequent code, per-code failures isolate into `{agent:'tick', error}` entries. POST only per contract — the Wave-0 stub's GET allowance is dropped. (3) **Honest server inputs**: MandateFees and edited priors are client-local in Wave 2 (D-028), so the server Pit Boss pass runs with empty fees + DEFAULT_STAGE_PRIORS + event-derived observed stats — its suggestions cite day-count evidence and can never mention ₪ (calibration-safe by construction); ₪-ranked Today lives client-side where fees exist, and post-G3 the fee ledger feeds the same `rankMoveTheMoney` signature unchanged. (4) **Uncalibrated mandates still rank**: risk signals (aging, silence, overdue feedback, offer decay) are money-free facts; excluding unfunded mandates would blind the daily loop exactly where fee capture lags. They rank with `ev:null`, `evAtRisk:0`, `calibrated:false`, always AFTER calibrated items (sort: evAtRisk desc → riskWeight desc → dealId asc, deterministic ties), and no ₪ appears in any of their surfaces (tested). evAtRisk = EV × Σweights uncapped — compounding risks may exceed EV; it is a priority score denominated in ₪, each weight cited per-reason. Weights: aging = overshoot/median (cohort median of live deals in-stage; a solo deal defines its median and never self-flags), feedback = (days−6)/6 past Submitted/ClientInterview, follow-up = daysSilent/7 at ≥3d (detection shared with sla.ts — one silence rule), offer decay = (days−2)/5. (5) SLA sweep skips: existing episode id (any status), a still-pending outreach_runner draft on the same deal (never stack two pending follow-ups), tombstoned persons; follow-up text delegates to the existing `followUpDraft` (BiDi-sanitized, grounded) with the numeric episode claim appended. Typecheck clean (api files checked in isolation — tsconfig includes src only; the one PipelineView.tsx error is kanban-ui's in-flight Wave-2 file); 58 new tests, full suite 570 green including spend-cap regressions; tick source test-pinned to never import the spend module or any LLM client.

## D-032 (kanban-ui) · 2026-07-31 · Money surfaces: lib-composed ranges, invoice-dated "this month", placeholder-preserving columns, always-prompt FeeCapture
Autonomous calls inside the Wave-2 kanban-ui batch. (1) Cards, columns and Money-Board lanes display EV as a RANGE (fee × `effectiveProbabilityRange`), honoring the contract's "display always as a range until blended" beyond the header footnote; the helper (`src/components/pipeline/money.ts#evRangeForDeal`) is pure COMPOSITION of the money lib — guard order mirrors `dealEV` (tombstone/Bench→null, Rejected→0, override→point) and a unit test pins the range midpoint to `dealEV` for every case, so UI figures cannot drift from platform-data's math. (2) Header "צפוי החודש" is deliberately invoice-based, not probability-based: Σ feeAmount over CALIBRATED mandates with an outstanding ('due'/'sent') invoice DATED in the current calendar month; no dated invoice ⇒ an honest "אין חשבוניות מתוארכות" with no ₪ and no 0 — inventing close-timing math would be fiction the contract forbids. (3) A column whose deals include no calibrated mandate keeps the Wave-1 "ΣEV —" placeholder verbatim (never ₪0-for-empty); the calibration hint in the money header contains zero ₪ characters — the leak-sweep suite (`calibration-sweep.test.tsx`) asserts the absence of '₪' across board/money/today surfaces with both uncalibrated shapes (no fee at all; complete fee but nothing past Screened). (4) FeeCapture opens on EVERY completed drop into Placed (not only when the fee is missing): placement is exactly when fee/invoice reality changes; prefilled, and Escape/cancel/backdrop mutate nothing. Its computed-fee preview renders only when the typed draft is complete (`feeAmount != null`) — the form is the calibration instrument, so the derived amount of the numbers being typed is shown, never a placeholder. (5) MoneyBoard lanes exclude Bench/Rejected (no probability model — the lib refuses, so does the view), order calibrated-first by EV midpoint desc then uncalibrated by title, and reuse the SAME DealCard component per the task list.

## D-033 (kanban-ui) · 2026-07-31 · Edit-before-accept stamped via pre-patch (store takes no opts — FIX filed); Pit Boss bridge flipped in-batch
Two structural calls. (1) The D-029 handoff requires kanban-ui to stamp `editedBeforeAccept` at accept time, but the Wave-2A store's `acceptSuggestion(id)` accepts no options and `addSuggestion` drops unknown fields (checked before building). `acceptSuggestionWithEdit` (src/views/Inbox/acceptDraft.ts) therefore pre-patches the still-PENDING suggestion in store state — new objects via spread, no in-place mutation, D-018 ride-ahead-of-type pattern — then immediately routes through `acceptSuggestion`, whose own `{...sug}` spread persists body+marker with the store's audit/undo/dirty machinery untouched. EVERY UI accept path (queue, Today approvals) goes through it: unedited accepts stamp `false` so `suggestionEditRate`'s denominator counts "not edited" as data, absence stays "not measured". An actual edit also writes a human `suggestion.edit` audit entry (before/after bodies) BEFORE the accept entry, and the edited body is what the wa.me glue hashes/renders — the accepted-draft link carries exactly what she approved (round-trip tested). `FIX(platform-data)` filed in BOARD-STATUS to absorb this into `acceptSuggestion(id, opts?)` and delete the helper. (2) `src/agents/pitboss.ts` landed mid-batch, so the Today ranking shipped against a null bridge was flipped to the LIVE ranker inside the same batch (`pitbossBridge.ts`): the bridge adapts store shapes (Person[] → `flattenContacts` ContactRef[], epoch-ms → Date) and narrows output to what the UI consumes ({dealId, evAtRisk, reasons[].claim}). The UI drops items for uncalibrated mandates defensively even though the ranker already zeroes their ₪ (belt + braces — evAtRisk is a ₪ figure), a throwing/absent ranker falls back to the Wave-1 aging ranking rather than taking Today down, and accepted drafts render as inert wa.me `<a href>` while PENDING drafts show a "טיוטה ממתינה" chip — an unapproved draft never becomes a send link (G4).

## D-034 (quality-gate) · 2026-07-31 · Wave-2 closure: hand-computed seam parity, adversarial calibration shapes, localhost-stub gate self-test, cron mismatch flagged not fixed
Five autonomous calls closing Wave 2. (1) Seam methodology: cross-surface money agreement is asserted against HAND-COMPUTED expectations derived from the exported constants (DEFAULT_STAGE_PRIORS, FEEDBACK_OVERDUE_DAYS), never by reading the lib back — so a drift in either the lib or a consumer fails the seam; a conservation identity (lane range midpoint == header qualified Σ + early-range midpoint) pins that no deal is double-counted or dropped across surfaces. (2) The calibration sweep went adversarial: eleven hostile mandate shapes at once (missing/zero/negative/NaN/Infinity amounts, incomplete percent, complete-fee-but-nothing-past-Screened, tombstoned or Rejected past-Screened journeys, and a corrupt fee `kind` smuggled through the store), each proven ₪-free on header/columns/cards/lanes/Today/inbox/report, with a repair-one-fee sensitivity control proving the sweep can actually see ₪. (3) `scripts/gate/tick-check.sh` validates a DEPLOYED tick with zero data movement — the valid-auth call sends `codes:[]` (an honest no-op by protocol); the 503 no-CRON_SECRET branch is documented as not remotely testable (server env cannot be unset from outside) and stays pinned by unit tests. The script is itself self-tested against a 127.0.0.1 ephemeral http stub — judged G2-compliant (G2 bans live Upstash/real `/api`; an in-process localhost stub is neither), and the exec is async because a sync exec would deadlock the stub sharing the event loop. (4) The full server→client loop test drives the REAL redisBridgeStore over an in-memory Redis and the real `pullV3` with an injected fetch, asserting the single-writer rail byte-for-byte (persons/deals/events/legacy sections identical after an agent write) and that server suggestions cross the wire ₪-free into the rendered inbox. (5) The Vercel-cron-is-GET vs POST-only-tick mismatch is FLAGGED (RUNBOOK §3, board status) rather than patched: adding a GET wrapper means creating `api/agents/**` source, which is agents-engine/lead territory — the two packaging options are written up for the lead's G1 call.

## D-035 (integrations) · 2026-07-31 · Thread parser: wall-clock-as-UTC, strip-all-BiDi, run-collapsed reply state, no planned no_reply; lexicon as typed keys + grammar functions
Autonomous calls inside the Wave-3A contract. (1) **Timestamps**: WhatsApp exports carry local wall-clock with no timezone, so `threadParser` encodes the parts as UTC ISO strings — deterministic in every test/runtime timezone and ordering-faithful; inventing an IL offset would be wrong across DST and buy nothing (store consumers do ordering + day-math). Month-first US exports are auto-detected via the impossible-month swap (day>12), otherwise day-first (IL locale) always wins; 2-digit years pivot at 2000; AM/PM (with iOS's U+202F narrow space) supported. Impossible dates (31.13, 30.2, 25:00) land in `skipped[]` as `bad_timestamp` — the parser NEVER throws. (2) **BiDi**: ALL direction marks (LRM/RLM/ALM + embeds/overrides/isolates) are stripped from parsed output, not just the embed controls — exports pepper LRM/RLM through timestamps, names, and media markers, and they poison her-alias matching and message hashes; direction stays a rendering concern (`dir="auto"`, D-017: never ADD marks). (3) **ApplyPlan honesty**: one `logContact` input per HER message (hash-faithful to the parsed text, `dedupeKey = ts|hash` so the UI can make paste-twice idempotent — application-time concern, parsing stays pure); candidate messages collapse to one `setReplyState('replied')` per consecutive RUN stamped at the run's last message — full reply history without one store event per bubble; applying calls in order reproduces `latest` (latest state wins). A her-last thread yields `latest: awaiting_reply` with NO `no_reply` store call planned: silence-aging is the SLA agent's clock-based judgment, not a paste-time fact — filing `no_reply` at paste time would double-report the same silence the SLA sweep already detects. Candidate media placeholders count as replies (a sticker is an answer). System lines (no `name: ` after the timestamp) skip as `system`; pre-header text skips as `orphan`. (4) **Lexicon shape** (`src/i18n/he.ts`): flat typed keys + `t(key, params?)` where params are BiDi-sanitized and unbound tokens stay VISIBLE (`{jobTitle}`) — fail-loud beats silent blanks; Hebrew pluralization is deliberately NOT templated (naive `{n} ימים` produces wrong Hebrew) — grammar lives as functions (`daysAgoHe`/`durationHe`/`daysCountHe`/`candidatesHe`), moved from drafts.ts/mandateReport.ts and re-exported for the Wave-1 surface. My modules (drafts, mandateReport) now consume the lexicon with BYTE-IDENTICAL output (pinned tests untouched, one polish: 11+ day counts now grammatically correct via `daysCountHe`); `sla.*`/`pitboss.*` keys ship byte-parity-tested as READY-TO-ADOPT — agents-engine's files are not mine to edit (CONFIG filed in BOARD-STATUS). `chrome.*` keys are CATALOGUED ONLY: full UI i18n is on the cut list, zero view rewiring.

## D-036 (platform-data) · 2026-07-31 · acceptSuggestion(id, opts?) absorption: opts-stamped marker, no-opts preserves the pre-patch, edit-audit-then-accept, boolean returns
Absorbs the D-033 pre-patch per the Wave-3 contract. (1) **Marker semantics**: when `opts` is provided (even `{}`), the accept ALWAYS stamps `editedBeforeAccept` — true iff `opts.editedBody` is present and differs from the current body (body replaced; the wa.me glue then renders the EDITED text), false otherwise. A **no-opts call = not edited** (stamps false) with ONE exception: a boolean marker already riding the record is preserved, not clobbered — that is exactly the legacy `acceptDraft.ts` pre-patch path, which stays fully working (all Wave-2 tests green, zero edits to kanban-ui files) until kanban-ui deletes the helper and re-points its two call sites in Batch B, after which the preserve branch is dead-but-harmless. (2) **Audit order**: an effective edit appends a human `suggestion.edit` entry (before/after bodies) BEFORE the `suggestion.accepted` entry — same chronology as the retired helper. (3) **Returns**: accept/dismiss now return boolean (true = state changed); the previous void signature means no caller breaks. (4) **Hardening**: tombstoned-pending suggestions now refuse resolution (previously only non-pending refused), matching the helper's refusal set. `editedBeforeAccept` still rides the persisted JSON ahead of the lead-owned type — TODO(lead) markers sit on the local `SuggestionWithEditMarker` type in pipelineStore.ts.

## D-037 (platform-data) · 2026-07-31 · Cascade tombstones every reference vector incl. evidence; retention purges in UTC with PII audit-scrub; silver = deep-then-rejected only; stats null-over-zero
Wave-3A autonomous calls. (1) **Cascade scope**: `deletePersonCascade(personId, {confirm:true})` tombstones the person, ALL its deals' live records, its deals' StageEvents, and every live suggestion referencing the person by `personId`, by `dealId` (tombstoned deals' ids included — they still lead back to the person), **or by `evidence[].sourceId`** — without the evidence vector a merge_person suggestion riding another person would keep citing the deleted person and fail the completeness sweep. Bench entry is purged off the tombstone; contacts are embedded on the person and die with it. Tombstone-only — physical removal belongs exclusively to purgeExpired. (2) **Audit shape**: ONE summary entry (`person.cascade_delete`, id lists in `after`) instead of N per-record entries — a wide cascade must not flush the 500-entry rotation. The append-only auditLog intentionally keeps referencing the person (recording the deletion is its job); the completeness sweep covers the four record collections + selector surfaces. (3) **Undo**: cascade pushes a full-snapshot UndoEntry (UndoEntry gained optional `stageEvents`) — one-click undo restores person (bench intact), deals, suggestions, and events as ordinary LWW edits. (4) **Retention**: `revital_v3_retention` stores whole months (fractions round UP — longer window purges less, the safe direction); 0/negative/invalid = off (default). `purgeExpired` computes the cutoff by **UTC calendar-month arithmetic** (deterministic across timezones/DST), physically removes only tombstones with `deletedAt` STRICTLY older than the cutoff, scrubs audit entries whose entityId was purged (their before/after snapshots carry PII — the trail leaves with the data), drops purged ids from dirtyIds/undoStack, and appends a counts-only `retention.purge` entry. Purge is deliberately NOT undoable. **Known limit (flagged to lead)**: purge is client-side; remote tombstones re-appear on pull (never as live data) until api/data v2 grows a server-side purge. (5) **Silver medalist** = at bench time some deal of the person reached Submitted+ BEFORE entering Rejected (moving deal's `from`-stage counts, robust to StageEvent rotation; tombstoned events ignored). An explicit Bench park of a deep-but-unrejected deal is NOT silver (still in play ≠ lost late); re-benching refreshes reason/timestamp but never downgrades an earned medal. `benchBySilver()` returns `{silver, others}`, newest bench first. (6) **agentAcceptStats(agent)**: accepted/dismissed/pending/resolved + `acceptRate = accepted/resolved`, null when nothing resolved — the Bench Sourcer throttle halves only on rate STRICTLY below 1/5. (7) **Type gap for lead**: wave3 names the Bench Sourcer output kind `bench_match`; SuggestionKind has no such member — tests use `rematch`, TODO(lead) filed to add `bench_match` before agents-engine Batch B.

## D-038 (agents-engine) · 2026-08-01 · Bench Sourcer: verbatim-grounded citations, pair-final idempotency, deal-derived open mandates, compounding throttle, transport-only LLM; GET cron wrapper reads nothing but budgetMs
Wave-3B autonomous calls inside the binding Bench Sourcer / cron-wrapper contracts. (1) **Grounding is verified, not requested**: the LLM's JSON is untrusted data — `parseBenchResponse` never throws, drops unknown/duplicate personIds, non-numeric scores and empty reasons, and enforces that every citation's `quote` appears VERBATIM (whitespace-normalized, BiDi-sanitized) in the cited person field (`name`/`benchReason`/`notes`/`profileSummary`); a match with zero surviving citations is discarded as ungrounded. "Why matched" evidence therefore cites only field content that provably exists; profileSummary citations point at the backing analysis record (sourceType `analysis`), the rest at the person. (2) **Idempotency is pair-final**: deterministic id `s_bench_{personId}_{jobId}` — one suggestion per (person, mandate) ever; a dismissal stays final for the pair (planner skips existing ids in ANY status BEFORE spending tokens; the LWW merge is the backstop); kanban-ui's re-match CTA is the deliberate escape hatch. Suggestions carry personId only, never dealId — accepting is the human's move, card creation is the client's (single-writer, plan §3). (3) **Open mandates derive from deals; JD text from legacy savedJobs**: legacy JobDescriptions carry no `v`, so mandate change-tracking uses max live-deal `v` per jobId; OPEN ⇔ ≥1 live deal in Sourced..Offer AND no Placed/Paid deal (filled closes); the on-new-JD trigger (`newJdJobIds`) opens deal-less JDs by fiat and forces their re-pairing regardless of the cursor (run records trigger `event`). AgentStoreView gained READ-ONLY `jobs`/`analyses` refs (defensively mapped from blob top-level; write path byte-untouched — the single-writer rail is unchanged); the Supabase adapter returns honest empties (no legacy sections there — post-G3 decision). (4) **Incremental cursor**: pairs = (bench persons with v > cursor × all open mandates) ∪ (all bench persons × changed/new-JD mandates); cursor 0 = first run = full sweep; the cursor (blob vCounter) and the throttle volume ride bench agent_runs ahead of the lead-owned AgentRun type (D-018 pattern, TODO(lead) to fold in `volume` alongside `cursor`). (5) **Throttle compounds**: acceptRate STRICTLY < 0.2 halves the PREVIOUS run's recorded volume (5→2→1→1, floor 1, prev clamped to base); exactly 0.2 is healthy; null (nothing resolved) = full; recovery to ≥0.2 restores base immediately. Server-side rate comes from `benchAcceptStats` (pure mirror of the store's `agentAcceptStats` math over blob suggestion statuses). The volume caps what gets FILED (rank: score desc → silver medalist → personId; floor score 60), not what gets evaluated — evaluation is already cursor-bounded. (6) **Transport-only LLM, tick stays pinned clean**: `tick.ts` still contains zero spend/LLM strings (Wave-2 pin kept verbatim); the ONLY door is `api/_lib/benchTransport` — `checkAndCount(code,'claude')` BEFORE every call (tick and /api/analyze share one daily per-code budget), denial and upstream HTTP 429 both throw `BenchSpendCapError` → the pass stops, matches already gathered still file, and the AgentRun records outcome `capped` (`error` for other transport failures; the engine never throws). Batching: one mandate × ≤4 persons per call, budget checked before each call, the pass receives the tick's REMAINING budget, `partial` propagates for re-invocation. A plain POST tick without `benchPass`/`newJdJobIds` is behaviorally identical to Wave 2 — pinned by a spy test (transport factory never invoked). (7) **Cron wrapper attack surface ≈ zero**: `api/agents/cron.ts` is GET-only, same fail-closed guards in the same order as the tick (parity-tested across the full auth matrix), and reads NOTHING from the request except a defensively-parsed `?budgetMs=` — codes come from env TICK_CODES, benchPass is hardwired true, a cron URL cannot name codes or jobIds; vercel.json untouched (crons entry is G1, lead's move). (8) **Lexicon**: adopted the Wave-3A `bench.*` READY-TO-ADOPT keys in the new module (new code, no parity risk); deliberately did NOT swap `sla.*`/`pitboss.*` into the frozen deterministic modules this wave — byte-parity means the swap buys nothing now and churning pinned Wave-2 files in the final build wave is risk without reward (offer stays open for a maintenance pass). Typecheck clean (api files re-checked in isolation), full suite 797 green (61 files), 85 tests across the five touched/new files.

## D-039 (kanban-ui) · 2026-08-01 · Wave-3B UI: board-hosted approvals+digest, rematch-request rides the suggestion channel, restore un-benches via contract actions, three-source trail dedup, apply-time thread idempotency, export-gated cascade
Wave-3B autonomous calls. (1) **Pre-patch retired to an alias**: `acceptDraft.ts` no longer patches store state — both call sites (SuggestionsQueue, TodayView) now call `acceptSuggestion(id, { editedBody })` / `acceptSuggestion(id)` directly; the file keeps a `@deprecated acceptSuggestionWithEdit` thin alias delegating to the store because the Wave-2 suite (`acceptDraft.test.tsx`) imports it and pins the exact semantics D-036 absorbed — all 8 of those tests pass against the alias unchanged, proving the absorption byte-faithful. (2) **Zero new view keys again (D-024 pattern)**: the full `ApprovalsInbox` ships as a fourth board tab "אישורים"; the morning digest is a MODE inside it (title = the contract's "מה מחכה לך הבוקר", CTA = one tap back to inbox mode) — no App.tsx/lead wiring needed, standalone wiring stays an optional README contract. The Wave-1 `SuggestionsQueue` badge panel is deliberately untouched: quality-gate board-seam suites pin its testids, and a compact glance-panel + a full triage view are different jobs. Batch approve/dismiss is a loop over per-id store calls — no new mutation surface; batch accepts stamp `editedBeforeAccept:false` (measured, unedited). (3) **Re-match CTA files kind `rematch`, agent `bench_sourcer`**: the human REQUEST must never collide with the sourcer's OUTPUT (`bench_match`, pair-final ids per D-038) — `rematch` is exactly the escape hatch agents-engine designed for; the request is addressed to its fulfiller (addSuggestion audits `ai`+agent — the channel is agent-shaped by contract), guarded to one PENDING request per person, and its pendency is visible in the inbox as queued work she can dismiss to cancel. Evidence cites person fields (bench reason, silver medal). (4) **Restore-to-board semantics**: a parked (Rejected/Bench-stage) deal moves back to the `from` stage of the StageEvent that parked it (fallback Sourced when history rotated); live-pipeline-deal-only persons just un-bench; tombstoned-only history recreates a fresh deal on the last mandate at Sourced (honest re-entry, no invented progress); every path clears the bench flag via `upsertPerson` (audited person.update). Restore gets NO undo toast: it is a two-action compound (move + un-bench) and `undoLast` would half-revert it — a misleading undo is worse than none. Silver badge renders ONLY from stored bench metadata (never recomputed client-side). (5) **Digest ₪ discipline diverges from Today on purpose**: Today DROPS uncalibrated ranked items (it is a money list); the digest KEEPS them money-free (it is a morning worklist — hiding a real risk because its mandate lacks a fee would lie by omission; D-031 sanctions money-free ranking). ₪ renders only when the ranker's `calibrated` flag AND the UI's own `calibratedJobIdSet` agree (belt + braces). Overdue-feedback rows derive from Pit Boss's exported `FEEDBACK_OVERDUE_DAYS`/`FEEDBACK_STAGES` re-exported through `pitbossBridge` — the bridge stays the single kanban-ui↔agents seam, no thresholds re-declared. (6) **Trail = three sources, each truth told once**: StageEvents own moves (audit `deal.move`/`deal.create` rows excluded), Person.contactEvents own outreach (audit `person.contact`/`person.reply_state` rows excluded), auditLog contributes suggestion lifecycle (for suggestions targeting the deal directly or via its person) + undo + other deal-entity rows; suggestion-create rows carry evidence claims with sourceType chips. Trail timestamps render in UTC because thread-derived events encode wall-clock-as-UTC (D-035) — one zone per trail, ordering never lies. (7) **Paste-a-thread**: parse is pure and store-free; NOTHING mutates until the explicit apply button; application walks plan.calls in order through `logContact`/`setReplyState` only; idempotency lives at APPLICATION time per the parser contract — `contacted` dedupes on `ts|hash`, `replied` dedupes on ts; the raw paste is never persisted or transmitted (hashes only reach the store; G4 + privacy by construction). (8) **Cascade UI is triple-gated**: person picker → export button (typed-confirmation input stays DISABLED until the backup actually downloaded — "offered" upgraded to "enforced" because the cascade is the one destructive flow in the product) → exact-name typed confirmation → the store's own `{confirm:true}`; result shows CascadeResult counts + one-click undo (cascade snapshots). Retention purge button disabled while retention is off (no dead-end no-op). (9) **Mechanics**: `blobDownload`/`DownloadFn` extracted to `components/pipeline/download.ts` (BoardTools re-exports; avoids a BoardTools↔DataPanel import cycle); BenchRail extracted from PipelineView to `components/pipeline/BenchRail.tsx` (same testids — Wave-1/QG suites untouched). Typecheck clean; suite 823/823 green (65 files, +26 tests in 4 new suites); G4 gate exit 0.

## D-039 · 2026-08-01 · Eliran directive: G1-lite deferred into the G1 ceremony; CLI previews authorized
No main fast-forward and no Vercel Git-connect now. Previews unblocked via non-production `vercel deploy` from v3-jump (Eliran explicitly authorized CLI use for non-prod; `--prod` remains G1). Main-sync + Git-connect + vercel.json crons entry are packaged as ONE step inside the real G1 ceremony.

## D-040 · 2026-08-01 · Production source diff — exact result (Eliran's "probably isn't enough")
Downloaded the live production deployment's full source via the Vercel API (dpl_D1HQxkUJmeTtd4tc4tkqLynNDqJ2, CLI-deployed 2026-06-14) and diffed against `wip-fingerprint-drive`: IDENTICAL in every source file except `src/modules/profile-intel/profilePrompts.ts`, where the Drive/wip version carries a NEWER prompt revision (careerArc-style intelligence prompt) that was never deployed. Fast-forwarding main to wip-fingerprint-drive therefore upgrades that one prompt vs production; the G1 ceremony decides to ship it or pin the deployed variant.

## D-041 · 2026-08-01 · PRODUCTION OUTAGE FOUND: Upstash Redis is gone — cloud sync silently broken
`/api/data` on production returns 500 FUNCTION_INVOCATION_FAILED; runtime logs show `getaddrinfo ENOTFOUND supreme-racer-65345.upstash.io` — the Upstash database behind KV_REST_API_URL no longer exists. Consequences: (a) Revital's cross-device sync has been failing silently (the app swallows sync errors) since the DB vanished; her data lives ONLY in her browser's localStorage; (b) there is no live blob to snapshot — the G2 merge-rehearsal source becomes an Export-everything JSON from her browser (async, no meeting); (c) fix = provision a new KV/Upstash (G3 — Eliran click, free tier) + update env + redeploy (G1). Wave-2's blob-size guard and v3 merge land on whatever new store is provisioned. Also: the access code Eliran provided does not match the server's ACCESS_CODE (pulled securely via `vercel env pull`, kept only in gitignored .env.production.local; never logged).

## D-042 · 2026-08-01 · Eliran decision: seeding session REPLACED by progressive in-product seeding
Per-mandate first-open wizard (one fee field + backfilled cards presented for drag-to-true-stage); calibration lifts per-mandate only once seeded; baseline metrics auto-derived from existing history; the remaining questions become an async shareable text form, not a meeting. The invariant is unchanged and absolute: no ₪ renders for an unseeded mandate. Nothing on the critical path waits for a human meeting.

## D-043 · 2026-08-01 · Ops incident during preview unblocking (self-inflicted, cleaned)
First `vercel deploy` ran against the working tree carrying kanban-ui's in-flight edits (superseded); a botched worktree copy then auto-created a junk Vercel project `deploy-tree` with one deployment — deleted via API within minutes (204). Clean preview now deploys from `git archive v3-jump`: https://revital-ik7ivjtf7-elirank512-1022s-projects.vercel.app (behind Vercel Authentication; root 302 to login, v3 writes blocked). Lesson encoded: always deploy from an archived committed snapshot, never the live working tree.
